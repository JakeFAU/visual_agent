// Package storage persists graph documents on disk.
//
// The implementation is intentionally filesystem-backed and human-readable so
// saved workflows can be inspected, versioned, and backed up without a
// database.
package storage
