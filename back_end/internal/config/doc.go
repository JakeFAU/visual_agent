// Package config loads runtime configuration for the CLI and HTTP server from
// environment variables.
//
// Visual Agent intentionally keeps configuration small and explicit so local
// development, Docker usage, and deployment all share the same knobs.
package config
