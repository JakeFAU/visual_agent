// Package graph contains the shared workflow contract used by the frontend and
// backend.
//
// The structs in this package mirror the frontend Zod schema, while the
// validation helpers enforce the subset of the contract that v0 actually
// supports at runtime.
package graph
